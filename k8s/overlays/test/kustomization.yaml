apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Disposable local CI and restore-rehearsal variant. Keep production-only
# image, origin, replica, and rollout patches in overlays/server instead.
resources:
  - ../../base

patches:
  - path: room-template.yaml
